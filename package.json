{
  "name": "kafka-training-docs",
  "version": "1.0.0",
  "description": "Cloudflare Workers deployment for Kafka Training documentation",
  "private": true,
  "scripts": {
    "deploy": "mkdocs build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.4",
    "wrangler": "^4.14.4"
  }
}
